SSI_war = {
	allowed = {
        original_tag = SSI
    }
	enable = {
        country_exists = BAK
		SSI = {
			has_war_with = BAK
		}
	}
	ai_strategy = {
		type = front_control
		#tag = BAK			# target against a specific country
		state = 623				# target on a state
		priority = 10 # default 0, higher prio strats will override lower
		ordertype = invasion # front or invasion. if set this strategy will only apply to that specific order type
		execute_order = yes # yes or no. if set will override execute or not decision of front
	}
	ai_strategy = {
		type = naval_invasion_focus
		value = 100
	}
	ai_strategy = {
		type = invade
		id = BAK
		value = 100
	}
	ai_strategy = {
		type = invade
		id = MIT
		value = 100
	}

}